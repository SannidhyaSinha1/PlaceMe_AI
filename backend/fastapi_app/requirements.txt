# ── Web framework ──────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic[email]==2.9.0
pydantic-settings==2.6.0
python-multipart==0.0.12
python-dotenv==1.0.1
httpx==0.27.2

# ── Databases ──────────────────────────────────────────────────────────
asyncpg==0.30.0
sqlalchemy==2.0.50          # 2.0.43+ adds Python 3.14 support (2.0.36 breaks on 3.14)
greenlet==3.1.1             # required by SQLAlchemy async
aiosqlite==0.20.0            # local-dev fallback driver
motor==3.6.0
alembic==1.18.5             # schema migrations (boot falls back to create_all without it)

# ── Auth ───────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0
limits==5.8.0               # rate limiting (in-memory; boots fine without Redis)

# ── Gmail / Google ─────────────────────────────────────────────────────
google-auth==2.36.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.154.0

# ── LangChain + free LLMs ──────────────────────────────────────────────
# Pin langchain-core explicitly: leaving it transitive let it drift to a
# version incompatible with langchain-groq/google-genai, silently disabling
# the LLM. Keep these five in lockstep on the 0.3.x line.
langchain==0.3.7
langchain-core==0.3.63
langchain-groq==0.2.1
langchain-google-genai==2.0.4
langchain-community==0.3.7
langchain-huggingface==0.1.2

# ── Local vector store + HF models (optional heavy extras) ─────────────
faiss-cpu==1.9.0
transformers==4.46.0
sentence-transformers==3.2.1
torch==2.5.0

# ── ML / analytics ─────────────────────────────────────────────────────
scikit-learn==1.5.2
xgboost==2.1.2
pandas==2.2.3
numpy==2.1.3
matplotlib==3.9.2

# ── Web search ─────────────────────────────────────────────────────────
tavily-python==0.5.0

# ── File storage ───────────────────────────────────────────────────────
cloudinary==1.41.0

# ── PDF in/out ─────────────────────────────────────────────────────────
pdfplumber==0.11.4
reportlab==4.2.5

# ── Background jobs ────────────────────────────────────────────────────
celery==5.4.0
redis==5.2.0
